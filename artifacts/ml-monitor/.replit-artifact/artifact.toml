kind = "web"
previewPath = "/"
title = "ML Model Monitor"
version = "1.0.0"
id = "artifacts/ml-monitor"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21781

[services.development]
run = "pnpm --filter @workspace/ml-monitor run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ml-monitor", "run", "build" ]
publicDir = "artifacts/ml-monitor/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21781"
BASE_PATH = "/"
